Twttr ( Spenny Assignment )

Before starting the application please follow these steps :

1. npm install
2. Insert MongoDB URI in root/config.env

The application expects a MongoDB URI in enviroment file (config.env).
